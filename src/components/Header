import logo from "../assets/response-line-ai-logo.png";

export default function Header() {
  return (
    <header style={styles.header}>
      <img src={logo} alt="Response Line AI logo" style={styles.logo} />
    </header>
  );
}

const styles = {
  header: {
    width: "100%",
    padding: "24px 40px",
    display: "flex",
    alignItems: "center",
    backgroundColor: "#ffffff",
  },
  logo: {
    height: "40px",
  },
};
